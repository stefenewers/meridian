{
  "name": "meridian",
  "private": true,
  "version": "0.1.0",
  "description": "Pre-production experimentation for autonomous ride-hail fleet operations.",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run dev --workspace @meridian/web",
    "build": "npm run build --workspace @meridian/web",
    "start": "npm run start --workspace @meridian/web",
    "lint": "npm run lint --workspace @meridian/web",
    "typecheck": "npm run typecheck --workspace @meridian/web"
  }
}
